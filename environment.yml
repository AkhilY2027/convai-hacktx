name: mfa-dev
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - setuptools
  - wheel
  - openfst
  - kaldi
  - numpy
  - scipy
  - libsndfile
  - sox
  - pip:
      - montreal-forced-aligner==2.2.17
      - praatio>=6.3
      - textgrid
      - soundfile
